stelligent-puppet-utils
=======================

A collection of useful Puppet tools

Functions
=========

uniq\_hostname(basename) - returns a string comprised of basename with the
    host's IP address in 4 byte integer format appended to the end

